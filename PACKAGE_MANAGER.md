# 包管理器说明

## 📦 统一使用 npm

本项目**统一使用 npm** 作为包管理器。

## ⚠️ 重要提示

### 已删除的文件
- `pnpm-lock.yaml` - 已删除，避免与 npm 冲突

### Lock 文件管理
- ✅ `package-lock.json` - npm 的 lock 文件，**请保留**
- ❌ `pnpm-lock.yaml` - pnpm 的 lock 文件，**已删除**
- ❌ `yarn.lock` - yarn 的 lock 文件，**已添加到 .gitignore**

## 命令使用

### 安装依赖
```bash
npm install
```

### 添加新依赖
```bash
npm install <package-name>
```

### 添加开发依赖
```bash
npm install --save-dev <package-name>
```

### 删除依赖
```bash
npm uninstall <package-name>
```

## 为什么统一使用 npm？

1. **避免冲突**: 同时存在多个 lock 文件会导致依赖版本不一致
2. **团队协作**: 统一的包管理器确保所有成员使用相同的依赖版本
3. **CI/CD 一致性**: 构建环境使用相同的包管理器配置

## ⚠️ 请勿混用

请不要在项目中使用以下命令：
- ❌ `pnpm install`
- ❌ `yarn install`
- ❌ `bun install`

如果使用其他包管理器安装依赖，可能会导致：
- 依赖版本不一致
- 构建失败
- 运行时错误

## 检查 Lock 文件状态

如果发现项目中出现了其他包管理器的 lock 文件，请：
1. 删除该文件（如 `pnpm-lock.yaml` 或 `yarn.lock`）
2. 运行 `npm install` 重新生成 `package-lock.json`
3. 提交更改到 git

